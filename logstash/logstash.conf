input {
  beats {
    port => 5044
  }
}

filter {
  # Las etiquetas (labels) de Loki deben ser de baja cardinalidad:
  # usamos service y log_type, no el mensaje completo.
}

output {
  loki {
    url => "http://loki:3100/loki/api/v1/push"
    message_field => "message"
    tenant_id => "nutrialianza"
  }
}
